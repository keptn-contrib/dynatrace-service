apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dynatrace-service.fullname" . }}-test-connection"
  labels:
    {{- include "dynatrace-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dynatrace-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
